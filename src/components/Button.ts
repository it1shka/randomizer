import styled from 'styled-components'

const Button = styled.button`
  min-width: 60px;
  min-height: 30px;
  font-size: inherit;
  border: none;
  font-weight: bold;
  border-radius: 10px;
  padding: 0em 0.25em;
`

export default Button